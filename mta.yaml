_schema-version: 3.3.0
ID: zcapm3cudfior
description: A simple CAP project.
version: 1.0.0
modules:
- name: zcapm3cudfior-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: zcapm3cudfior-db
  - name: zcapm3cudfior-auth
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: zcapm3cudfior-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: zcapm3cudfior-db
  parameters:
    buildpack: nodejs_buildpack
- name: zcapm3cudfior-destination-content
  type: com.sap.application.content
  requires:
  - name: zcapm3cudfior-destination-service
    parameters:
      content-target: true
  - name: zcapm3cudfior_html_repo_host
    parameters:
      service-key:
        name: zcapm3cudfior_html_repo_host-key
  - name: zcapm3cudfior-auth
    parameters:
      service-key:
        name: zcapm3cudfior-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zcapm3cudfiorapprouter_zcapm3cudfior_html_repo_host
          ServiceInstanceName: zcapm3cudfior-html5-app-host-service
          ServiceKeyName: zcapm3cudfior_html_repo_host-key
          sap.cloud.service: zcapm3cudfiorapprouter
        - Authentication: OAuth2UserTokenExchange
          Name: zcapm3cudfiorapprouter_zcapm3cudfior_auth
          ServiceInstanceName: zcapm3cudfior-auth
          ServiceKeyName: zcapm3cudfior-auth-key
          sap.cloud.service: zcapm3cudfiorapprouter
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: zcapm3cudfior-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zcapm3cudfior_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zcapm3cudfiorui.zip
      name: zcapm3cudfiorui
      target-path: resources/
- name: zcapm3cudfiorui
  type: html5
  path: app/zcapm3cudfiorui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zcapm3cudfior-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: zcapm3cudfior-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: zcapm3cudfior-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: zcapm3cudfior-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zcapm3cudfior-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: zcapm3cudfior_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zcapm3cudfior-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
